---
import SalesConfiguratorPage from "@/blocks/SalesConfiguratorPage.astro";
import { getLangFromUrl } from "@/i18n";

const lang = getLangFromUrl(Astro.url);
---

<SalesConfiguratorPage locale={lang} />
